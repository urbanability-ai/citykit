# secrets
.env

# generated outputs
artifacts/
data/
derived/
provenance/

# local reference images (not committed)
inputs/reference/
inputs/reference/**

# archives
*.zip

# keep repo light by default (adjust later if needed)
*.mp4
*.mov
.DS_Store
